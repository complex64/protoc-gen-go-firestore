# Proto files which include `firestore/options.proto`
# compile to Go code that imports "github.com/complex64/protoc-gen-go-firestore/firestorepb";
# Generate the package:
version: v1
plugins:
  - name: go
    out: .
    opt: paths=source_relative
